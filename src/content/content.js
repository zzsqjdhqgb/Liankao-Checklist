function Init() {
    AddElement();
    AddListener();
    // await setStorage({key1: "val1"});
    // await setStorage({key1: "val2", key2: "val3"})
    // await getStorage();
}

console.log("ins");

document.addEventListener("DOMContentLoaded", Init);